services:
  - type: web
    name: farma-basica
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PROFESSOR_KEY
        generateValue: true
      - key: NODE_VERSION
        value: "20"
